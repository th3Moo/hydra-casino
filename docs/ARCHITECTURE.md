# Hydra Casino Architecture

- Go microservices for auth, wallet, transactions.
- Node.js proxy for CashApp integration.
- Nginx for routing & frontend hosting.
- docker-compose for orchestration.
- Secrets managed via `.env`.

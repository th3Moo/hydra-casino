# Hydra Casino Gateway

### Run locally
```bash
docker-compose up --build
```

Visit: [http://localhost](http://localhost)

### Services
- Auth → `/auth`
- Wallet → `/wallet`
- Transactions → `/tx`
- CashApp → `/cashapp`
